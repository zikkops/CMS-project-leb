<?xml version="1.0" encoding="utf-8"?>
<!--
  The launch screen: the icon, centred, on the café's colour. It replaces
  Capacitor's default splash images, one PNG per density (UPGRADE.md T1.26).
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_primary" />
    <item
        android:width="160dp"
        android:height="160dp"
        android:gravity="center"
        android:drawable="@drawable/ic_launcher_brand" />
</layer-list>
